# Toolset scaffold and local knowledge verification

This bounded #19 child covers PT-01 and the local knowledge/unavailable-lifecycle
parts of PT-04. It consumes native harness candidate `519b199` and common connection
candidate `ab7cecb`; their integration and independent review belong to root.
The common verification portability correction `6ca7b6e` was consumed locally as
`dbd8f83`. No tracker or remote service state was changed.

## Delivered behavior

A pure trusted-definition plan validates provider roles and renderer-supported
client selections. Real adoption and initialization persist explicit tracker,
knowledge and repeated agent-client selections independently of language preset.
Omitted options retain Linear, Claude Code/Codex and existing finish gates. Copier
answers retain selections during staged updates and authored conflicts remain
protected. Definitions own non-secret scaffold defaults; the Jira definition
becomes selectable after its separate candidate is integrated, without fabricated
identity, credentials or CLI vendor branching.

Obsidian's schema-1 component declares existing note guidance and no required GUI
module. Bounded trusted requirements inspect only the runtime `paths.vault`
directory. Missing paths, non-directories and provider-local substitutes cannot
claim readiness. Optional desktop viewing is separate, including headless mode;
inspection reads no note content, creates nothing and does not test write access.
Plane guidance can be delivered, but its absent AI-DLC lifecycle adapter remains
explicitly blocked. GitHub/Jira selections do not acquire a Plane prerequisite.

## Test-first evidence

- Initial selection tests exposed 12 failures: absent pure plan/client arguments,
  rejected declared alternatives and missing repeated CLI options. The first
  implementation passed all 17 focused selection/real-Copier cases.
- Six readiness cases then failed because vault/lifecycle distinctions were absent.
  The implementation passed those cases. Existing exact catalog snapshots were
  updated for the two added components without changing schema 1.
- An additional compatibility regression caught an unnecessary `kind=linear` in
  omitted defaults. Removing that addition preserved the original provider table.
- The combined scaffold/readiness/component/connection/CLI suite passed 114 tests.
  Real native rendering uses the existing adapter and asserts Claude/Antigravity
  files; it does not establish installed-client recognition or login.
- Initial required checks passed four categories and 1,208 tests, with one source
  distribution failure from an inherited personal machine path in the common
  connection report. The accepted documentation correction removed that path;
  the distribution guard was retained unchanged.

All five required checks then passed, including **1,209 tests**. Strict OpenSpec
validation passed. The [local receipt](toolset-composition/local-checks.json)
identifies the dirty candidate based on `dbd8f83` before the final evidence commit;
it is not an exact merged-revision receipt. Source, tests and assets stayed frozen
through the passing run. Bootstrap used a task-specific supported home and local
uv 0.9.11; the shared bootstrap runtime was not replaced.

## Explicit remaining scope

PT-02 is supplied by the separate common connection child. This child selects
clients but does not derive or deduplicate native provider server bindings;
PT-03 composition through `agents.servers` is a separate remaining child.
Existing native rendering still consumes explicit server definitions. Account
references never prove native OAuth identity, and client/account/live platform
qualification remains unverified. No upstream native URL, Jira account setup or
Plane lifecycle support is invented. Parent #19 must not be marked complete from
this child's local checks alone. Root owns review, dependency integration,
archival, PR/CI and configured finish evidence.

## Independent review correction: Copier update boundary

Review found that update replay accepted retained or tampered selections without
using the trusted definition/client checks. Five real versioned-Copier regressions
failed before the fix: unknown tracker, knowledge and client selections, a changed
tracker-settings kind, and an unsupported new default introduced during update.
Both retained and staged answer files now pass the shared selection contract before
destination writes. Valid legacy defaults and previously selected fragments stay
compatible; invalid data is refused with the original destination bytes intact.
The focused real update/compatibility suite passed 13 tests. YAML parsing reuses
Copier's existing required PyYAML dependency; no package or lockfile changed.

The update-boundary repair passed **1,214 tests**, four required checks and
strict OpenSpec validation; lint failed on the saved-answer parsing exception
contract. An earlier prose summary incorrectly described all five as passing. Its
[receipt](toolset-composition/review-local-checks.json) records the dirty repair
candidate based on `1904e9c`; source/tests/assets were frozen during that
run. The later combined repair below corrects the lint failure. Independent
re-review and merged-revision delivery remain pending.

## Staged shared-configuration validation

Two further real versioned-template regressions reproduced update applying a
machine-only path or a literal credential field despite valid saved selections.
The staged manifest now passes the normal project-layer scope/credential validator
after Copier conflict detection and before destination writes. A third regression
preserves legitimate authored provider configuration, including its environment
reference. The focused update/compatibility suite passed 16 tests. No new secret
format, package, provider operation or ownership rule was introduced.

The first complete run passed 1,217 tests and four required checks; lint rejected
the saved-answer parsing boundary raising `ValueError` for malformed external
input. That boundary intentionally uses the existing CLI validation error contract,
so the rule is locally suppressed rather than changing the exception contract.

The corrected frozen source passed **all five required checks with 1,217 tests**.
The [complete passing receipt](toolset-composition/scope-review-checks.json) records
the dirty candidate based on `11f61f6`; the [first staged-validation run](toolset-composition/scope-review-first-checks.json)
and earlier update-boundary receipt remain available with their actual lint failures.
Strict OpenSpec validation also passed. Root's independent source review found no
additional issues in the combined repair; integration and exact merged-revision
verification remain root-owned delivery steps.
