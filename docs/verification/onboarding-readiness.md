# Work-computer onboarding readiness repair

This bounded OR-01–03 follow-up arose from an actual CLI walkthrough of 4064bd9.
A disposable Python project selected Jira Cloud, Obsidian, Claude Code and
Antigravity; real preview/apply, reviewed native composition and render/check
preserved application, AGENTS and authored MCP sentinels. Missing Jira metadata
and credentials remained blocked, and client recognition/authentication stayed
unverified. No service account or native process was used.

The walkthrough exposed false unsupported-component diagnostics for the standard
scm=github and deploy=none choices, and an incomplete machine-vault instruction.
A new bounded specification records the repair without editing archived PT-04.

## Runtime and metadata repair

Two packaged schema-1 components now declare GitHub SCM (existing core git/gh
requirements and SCM guidance) and inactive deployment (no module or health
qualification). Trusted runtime metadata checks scm.repository with exactly the
GitHubSCM owner/repo syntax; provider-local substitutions do not satisfy it.
Readiness interprets these definitions without vendor branches. Unknown and
role-incompatible providers still block, available binaries do not imply gh
login, and local configuration does not establish repository access or CI.

Initial new cases reported **10 failed, 4 passed**. The implementation then passed
100 affected cases with three failures solely from exact packaged-catalog
snapshots lacking the two intentional entries. Those snapshots were updated,
retaining exact schema/asset assertions. The final focused results are recorded
below. Existing real Copier selection/update compatibility cases passed in the
first affected run; they were not broadly repeated after snapshot-only changes.

## Actual existing enrollment route

Using the same disposable target and isolated XDG config/cache/state directories,
the real CLI enrolled a minimal local Git profile containing schema4 and the
explicit profile_id work-cli-profile. An initial raw-SHA request was refused
because enrollment requires an advertised ref. The reviewed tag reviewed-v1,
pointing to bab70196b60c83e182fb71870ad3e8405eba75ba, then previewed and applied
successfully. The output identified portable=false, no credentials, no global
client changes, and the exact machine path ending in
ai-dlc/machines/work-cli-machine.toml.

Only that emitted machine file was edited to bind the pre-existing temporary
vault. Ordinary profile show then attributed paths.vault to machine, and ordinary
project readiness reported the Obsidian directory ready while preserving project
Jira/client choices and unverified authentication. No new --machine CLI option,
enrollment format or user-agent apply was needed. Enrollment's existing global
ownership preview was read-only and reported no changes; actual persistence stayed
inside isolated XDG paths. No real home/client configuration was changed.

The repaired CLI was then exercised again in that disposable project: missing
SCM configuration produced the actual scm.repository next step, the none role was
inactive, and after adding a synthetic fixture/work-code SCM identity its local
configuration check became ready. The isolated PATH still lacked gh, so the tool
requirement correctly remained missing. Jira configuration/credentials remained
missing. Native rendering preview/apply/check succeeded again, and all three
authored sentinels survived. This is a real local CLI/filesystem walkthrough, not
real Jira/GitHub authentication, native session recognition or deployment evidence.

The runbook now provides the existing enrollment/profile/tag/machine-file path
and concrete SCM table, preserving legacy settings and keeping private vaults
outside shared project files. The original exact argv/stdout/stderr logs and
inspection are retained in the disposable walkthrough report at /tmp during this
review session; portable documentation contains no personal machine path.

## Verification boundary

Prepared source Python3.12.11 and AI-DLC0.4.0 on existing macOS15.3.2 arm64 were
reused. No source bootstrap, download, package build, full suite, company call,
service mutation or native login was performed. Prior coordinator all-five results
apply to 4064bd9, not this repair. Independent review, final integrated required
checks, archive/PR/merged-CI and work finish remain coordinator-owned.

Final focused verification passed **109 tests in 0.75 seconds** across onboarding,
readiness, component catalog and credential requirements. Repository-wide format
and lint checks passed; prepared-interpreter type validation reported zero errors,
warnings or information messages. Source agents render --check and the generated
asset checker passed after owned guidance regeneration. Strict validation of the
new onboarding-readiness change passed. Only evidence/task metadata was finalized
after these checks; no full integrated success is claimed.
