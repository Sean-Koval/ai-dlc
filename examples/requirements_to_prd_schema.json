{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Product Requirements to User Stories and PRD Schema",
  "description": "Schema for transforming product requirements into user stories and a simple PRD",
  "type": "object",
  "required": ["product_name", "requirements"],
  "properties": {
    "product_name": {
      "type": "string",
      "description": "Name of the product being documented"
    },
    "requirements": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "Individual product requirement statements"
      },
      "description": "List of product requirements to be transformed"
    },
    "target_audience": {
      "type": "string",
      "description": "Description of the intended users of the product"
    },
    "priority_level": {
      "type": "string",
      "enum": ["low", "medium", "high", "critical"],
      "description": "Overall priority level for the requirements"
    },
    "additional_context": {
      "type": "string",
      "description": "Any additional context about the product or requirements"
    }
  }
}
