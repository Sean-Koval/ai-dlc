{
  "$defs": {
    "Client": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "claude-code",
          "title": "Kind",
          "type": "string"
        },
        "sha256": {
          "additionalProperties": {
            "pattern": "^[0-9a-f]{64}$",
            "type": "string"
          },
          "minProperties": 1,
          "propertyNames": {
            "enum": [
              "linux-x64",
              "linux-arm64"
            ]
          },
          "title": "Sha256",
          "type": "object"
        },
        "version": {
          "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$",
          "title": "Version",
          "type": "string"
        }
      },
      "required": [
        "kind",
        "version",
        "sha256"
      ],
      "title": "Client",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "The image both arms share: a pinned parent, distribution packages and one client.",
  "properties": {
    "client": {
      "$ref": "#/$defs/Client"
    },
    "from": {
      "pattern": "^([^\\s@]+@)?sha256:[0-9a-f]{64}$",
      "title": "From",
      "type": "string"
    },
    "packages": {
      "default": [],
      "items": {
        "pattern": "^[a-z0-9][a-z0-9+.-]*$",
        "type": "string"
      },
      "title": "Packages",
      "type": "array"
    },
    "schema": {
      "const": 1,
      "title": "Schema",
      "type": "integer"
    }
  },
  "required": [
    "schema",
    "from",
    "client"
  ],
  "title": "BaseImage",
  "type": "object"
}
