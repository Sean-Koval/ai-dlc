schema = 1
id = "packaging-force-includes"
title = "Stop packaging asset directories that nothing reads from the installed wheel"
scope = "Remove the playbook and contracts force-includes from the wheel build: no code resolves either through ai_dlc.files.assets, the contract schemas are generated from the Python contracts module and consumed from the project root, and the playbook is documentation. Both directories stay in the repository and the source distribution. Extend the distribution test so every force-included asset directory has a reader."
requires_spec = false
spec_reason = "Packaging of unread static files only; no command, contract or configuration behaviour changes."
acceptance = [
  "Every force-included wheel directory has at least one assets(...) reader in src/ai_dlc, and a test proves it.",
  "The wheel no longer contains ai_dlc/assets/playbook or ai_dlc/assets/contracts; the source distribution still carries both directories.",
  "All required checks pass."
]
reviewed = true
depends_on = []

[providers]
specs = "openspec"
tracker = "github-issues"
knowledge = "obsidian"
scm = "github"
deploy = "none"

[artifacts]
tracker = "101"
branch = "work/packaging-force-includes"
