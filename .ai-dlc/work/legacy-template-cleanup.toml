schema = 1
id = "legacy-template-cleanup"
title = "Reduce the legacy Claude template to the files AI-DLC actually authored and describes"
scope = "Delete the community-sourced commands and hook configurations, the empty gemini and roo templates and the dangling workflow documents from the packaged legacy scaffold assets; rewrite the AI-DLC-authored README, CLAUDE.md and settings.json so they describe only what ships, wiring the three shell hooks through Claude Code's real settings schema; point the scaffold test at real content. The scaffold command, its release gate and the current project templates are unchanged."
requires_spec = false
spec_reason = "Packaged static assets only; the scaffold command's observable contract (copy a provider tree, refuse conflicting content) is unchanged and remains tested."
acceptance = [
  "Every command, hook and file named in the template's README, CLAUDE.md and settings.json exists in the tree.",
  "ai-dlc scaffold --all into an empty directory produces no empty files, and a test proves it.",
  "No packaged template file is copied from an external collection or references removed collections, /sc: commands or cargo.",
  "All required checks pass."
]
reviewed = true
depends_on = []

[providers]
specs = "openspec"
tracker = "github-issues"
knowledge = "obsidian"
scm = "github"
deploy = "none"

[artifacts]
tracker = "92"
branch = "work/legacy-template-cleanup"
