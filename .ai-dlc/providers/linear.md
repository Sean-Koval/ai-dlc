# Linear provider guidance

Use the selected Linear provider only for the configured team and status
mappings. Treat credentials as local environment references and keep account
selection explicit.

Provider reads can inform work, but project checks and finish gates retain their
own authority.
