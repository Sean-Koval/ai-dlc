## Why

The current work-computer request explicitly selects Claude and Antigravity. Only
Codex/Claude Code are implemented, and project Claude remote MCP definitions omit
its required HTTP type. A cloned engine must prepare each work repository with
its own tools and guidance without borrowing the personal AI-DLC tracker.

## What Changes

- Add opt-in project Antigravity skill, rule and MCP rendering under existing ownership.
- Emit correct Claude HTTP definitions while preserving authored entries.
- Report delivered configuration separately from actual client recognition/login.
- Supply a work-repository setup and qualification runbook.

## Capabilities

### New Capabilities
- `native-work-harnesses`: project Claude/Antigravity guidance and MCP setup.

### Modified Capabilities
None. Defaults and lifecycle completion gates remain unchanged.

## Impact

Project renderer, readiness, tests and docs. This is a bounded child of GitHub #19.
Global/personal Antigravity configuration, automatic installation, undocumented
legacy formats, new hook support, Jira lifecycle implementation and Confluence
are separate work. Current project-native support does not qualify a live laptop.
