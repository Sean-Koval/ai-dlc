# Reconcile canonical documentation and OpenSpec lifecycle

## Decisions

This is an editorial change. Runtime document services, mandatory checks and
private-vault behavior are unchanged. GitHub native issue state and closure reason
own status; board Done alone is insufficient. OpenSpec owns formal requirements
and archive history; docs retain durable explanations and evidence.

DL-01 reconciles roadmap, product direction, release navigation and handoffs using
read-only GitHub observations. Issues 14–17 and 20–22 are cancelled, not completed.
Historical checkpoints remain clearly labelled rather than deleted. Missing live
qualification remains missing even when its former ticket is cancelled.

DL-02 compares PR28 DK-01–07 with the merged Python implementation and focused
regressions, then uses OpenSpec archive to publish the canonical specification.
The old work record follows the archive. Seventeen preexisting autogenerated
Purpose placeholders and the newly archived DK Purpose describe actual requirements;
requirement bodies and historical Rust implementation remain intact.

DL-03 retains the optional catalog as the current navigation inventory and writes
one explicit baseline evidence record in docs/verification/documentation-baseline.json.
Each initial diagnostic records path, code, initial/final source digest, responsible
role and a reason. Eleven actual archive links are repaired; five illustrative
historical references remain visible. Four current/status documents enter the
catalog; remaining uncatalogued material is individually dispositioned without
claiming factual review. Unknown review dates stay unknown. The existing checker
does not consume this evidence record or suppress its diagnostics.

## Verification and boundaries

- Inspect all initial 101 uncatalogued, 16 broken-link and five unknown-review findings.
- Re-run check_documents with a fixed date and require every remaining diagnostic
  to match a disposition and current source digest; do not demand a false clean result.
- Verify canonical Purpose coverage and archive/work links; run strict OpenSpec validation.
- Run the existing 46 focused document/portal/private-knowledge regressions for
  PR28 reconciliation. No new executable behavior is introduced here.
- Controller owns independent review, manifest-required checks, integration and
  gated finish. PR-head CI, exact merged-revision receipts and live client evidence
  remain separate. No remote state mutation or package publication occurs here.

## Delivery evidence

Scoped and integrated independent review approved. All six required local checks passed with 2022 tests and8 skips before archival-only metadata finalization. See docs/verification/documentation-workflow.md for exact scope and live qualification limits.
