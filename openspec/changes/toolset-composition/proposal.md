## Why

PT-01 and PT-04 need explicit scaffold/client selections and honest local knowledge readiness after the common connection and native harness children. Current adoption hardcodes two trackers and two clients; an existing filesystem vault has no knowledge component.

## What Changes

- Add a trusted-definition-driven pure toolset plan consumed by adoption and initialization, including repeatable client selections.
- Preserve omitted legacy defaults, Copier answers, staged previews and authored conflict handling.
- Declare optional Plane as unavailable for lifecycle operations, and require an existing runtime vault directory for Obsidian note readiness independently of its optional GUI.

## Capabilities

### New Capabilities
- `toolset-composition`: Scaffold selected supported definitions and report bounded local capability requirements.

### Modified Capabilities
None. Component catalog schema 1, native rendering ownership and lifecycle operations remain unchanged.

## Impact

Templates, CLI arguments, trusted provider definitions, component metadata, readiness, guidance and regression tests. Consumes common connection and native harness candidate contracts. No account login, installation, service writes or Confluence integration.
