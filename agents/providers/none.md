# Deployment disabled

The `none` deployment selection intentionally disables this capability. It requires
no deployment tool, credential or service qualification. It does not declare an
implemented deployment adapter or prove that an application was deployed.
Tracker, specification, SCM and configured work-finish requirements remain in force.
